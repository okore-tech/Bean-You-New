import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  reactStrictMode: true
  // You can add image remotePatterns here later if you load external images
};

export default nextConfig;
